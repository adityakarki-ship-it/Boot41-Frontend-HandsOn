// TODO: Implement the Header component
//
// Requirements:
// - Use useSidebar() hook → toggleSidebar
// - Use useThemeMode() hook → { mode, toggleTheme }
// - Use useLocation() from react-router-dom for breadcrumbs
// - Render MUI AppBar (position="sticky", color="default", elevation={0})
//   with a bottom border
// - Inside Toolbar:
//   a. Menu IconButton (calls toggleSidebar)
//   b. Breadcrumbs: "Home" link + current path segments
//   c. Theme toggle IconButton: LightMode / DarkMode icon based on current mode
//
// MUI imports you'll need:
//   AppBar, Toolbar, IconButton, Typography, Box, Breadcrumbs, Link from '@mui/material'
//   Menu, LightMode, DarkMode from '@mui/icons-material'
// Also: useLocation, Link as RouterLink from 'react-router-dom'

export function Header() {
  // TODO: Implement the app header with menu button, breadcrumbs, and theme toggle
  return null;
}
